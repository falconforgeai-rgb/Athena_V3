# Athena_V3 — Evidence Engine Runtime Layer

The Athena_V3 repository is the public execution and validation environment for the FalconForge Codex integrity system (v3.5+).

It provides:
- Canonical schema + manifest validation
- CAP payload structural verification
- Integration with the Athena CAP Bridge API for civic audit logging

This repository contains **no secrets** and is safe for public audit.

